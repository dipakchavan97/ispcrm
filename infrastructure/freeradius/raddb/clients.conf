# Clients configuration for FreeRADIUS
client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}

client docker-backend {
    ipaddr = 172.22.0.0/16
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}

client docker-vpn {
    ipaddr = 172.23.0.0/16
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}
